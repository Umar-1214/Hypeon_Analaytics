# Hypeon Analytics — copy to .env and set values. Do NOT commit .env.

# Database (required for API)
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:5433/hypeon

# Raw data directory (CSVs from pipeline or dummy data)
DATA_RAW_DIR=data/raw

# Optional API key for production (X-API-Key or Authorization: Bearer <key>)
# API_KEY=

# CORS origins (comma-separated)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173,http://127.0.0.1:3000

# Log level: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO

# Scheduled pipeline run interval in minutes (0 = disabled)
PIPELINE_RUN_INTERVAL_MINUTES=0

# Optional: Copilot LLM (Gemini or OpenAI)
# GEMINI_API_KEY=
# GEMINI_MODEL=gemini-1.5-flash
# OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
